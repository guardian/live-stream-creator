A collection of utility scripts to run on instance boot.

# add-publisher
Adds a publisher

# enable-stream-targets
Switches on stream targets so wowza can push to an endpoint

# enable-transcoder
Switches on the transcoder to add graphics overlays to streams
